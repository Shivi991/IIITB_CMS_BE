---
- name: "Deploy a jar file"
  hosts: "172.16.144.226"
  tasks:
    - name: "Pull docker images"
      command: "docker-compose -f docker-compose.yml pull"
    - name: "Start docker compose"
      command: "docker-compose -f docker-compose.yml up -d"
